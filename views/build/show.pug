extends ../layout

block content
  .page-header
    a(href='/builds') &#8592; Back to builds
    h3
      div= build.name
    if build.draft
      small
        i.fa.fa-newspaper-o
        | Draft

  div(class='badge')= build.category

  p= build.desc
