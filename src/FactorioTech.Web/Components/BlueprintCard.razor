@using FactorioTech.Web.Core;
@using Humanizer;
@using System.Text;

@inject ImageService _imageService
@inject BlueprintConverter _blueprintConverter

<div class="card mb-1">
    <div class="card-header">
        <div class="row">
            <div class="col-md-8">
                <div class="media">
                    <BlueprintIcons Icons="Blueprint.Icons" Type="blueprint" />
                    <div class="media-body">
                        <h5 class="card-title mt-2 mb-0">@(Blueprint.Label ?? "(no title)")</h5>
                        <small class="text-muted">Contains @entitiesCount entities</small>
                    </div>
                </div>
            </div>
            <div class="col-md-4">
                <div class="btn-toolbar float-right" role="toolbar">
                    <div class="btn-group mr-2" role="group">
                        <button type="button" class="btn btn-sm btn-light border">
                            Copy to clipboard
                        </button>
                    </div>
                </div>
            </div>
        </div>
        <div class="row align-items-end">
            <div class="col">
                <ul class="nav nav-tabs card-header-tabs justify-content-end" role="tablist">
                    <li class="nav-item" role="presentation">
                        <a class="nav-link active" id="overview-tab-@metadata.Hash" data-toggle="tab" href="#overview-@metadata.Hash" role="tab"
                           aria-controls="overview" aria-selected="true">
                            Overview
                        </a>
                    </li>
                    @if (entitiesCount > 0)
                    {
                        <li class="nav-item" role="presentation">
                            <a class="nav-link" id="entities-tab-@metadata.Hash" data-toggle="tab" href="#entities-@metadata.Hash" role="tab"
                               aria-controls="entities" aria-selected="false">
                                Entities <span class="badge badge-info">@entitiesCount</span>
                            </a>
                        </li>
                    }
                    @if (metadata.Encoded != null)
                    {
                        <li class="nav-item" role="presentation">
                            <a class="nav-link" id="blueprint-tab-@metadata.Hash" data-toggle="tab" href="#blueprint-@metadata.Hash" role="tab"
                               aria-controls="blueprint" aria-selected="false">
                                Blueprint String <span class="badge badge-info">@Encoding.UTF8.GetByteCount(metadata.Encoded).Bytes().Humanize("#.##")</span>
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-4">
                <img src="/api/files/blueprint/@(metadata.Hash).jpg" class="img-thumbnail" loading="lazy" />
            </div>
            <div class="col-md-8">
                <div class="tab-content">
                    <div class="tab-pane fade show active" id="overview-@metadata.Hash" role="tabpanel" aria-labelledby="overview-tab-@metadata.Hash">
                        <p class="card-text">@(Blueprint.Description ?? "(no description)")</p>
                    </div>
                    @if (entitiesCount > 0)
                    {
                        <div class="tab-pane fade" id="entities-@metadata.Hash" role="tabpanel" aria-labelledby="entities-tab-@metadata.Hash">
                            <table class="table table-striped table-sm">
                                <tbody>
                                    @foreach (var (key, count) in GetEntityStats(Blueprint))
                                    {
                                        <tr>
                                            <td><img src="/api/files/icon/32/item/@(key).png" width="32" height="32" loading="lazy" /></td>
                                            <td>@count</td>
                                            <td><a href="@Utils.GetWikiUrlForEntity(key)">@key</a></td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    }
                    @if (metadata.Encoded != null)
                    {
                        <div class="tab-pane fade" id="blueprint-@metadata.Hash" role="tabpanel" aria-labelledby="blueprint-tab-@metadata.Hash">
                            <code>@metadata.Encoded</code>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@code {
    [Parameter]
    public FactorioApi.Blueprint Blueprint { get; set; } = null!;

    [Parameter]
    public BlueprintMetadataCache MetadataCache { get; set; } = new BlueprintMetadataCache();

    private BlueprintMetadata metadata = new BlueprintMetadata(string.Empty, string.Empty);
    private int entitiesCount;

    protected override async Task OnInitializedAsync()
    {
        entitiesCount = Blueprint.Entities.Count();
        await MetadataCache.EnsureInitialized(Blueprint);

        metadata = MetadataCache[Blueprint];
    }

    public IDictionary<string, int> GetEntityStats(FactorioApi.Blueprint blueprint) =>
        blueprint.Entities
            .GroupBy(e => e.Name)
            .OrderByDescending(g => g.Count())
            .ToDictionary(g => g.Key.ToLowerInvariant(), g => g.Count());

}
