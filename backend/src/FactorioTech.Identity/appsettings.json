{
  // IMPORTANT!
  // The configuration in this file is ONLY respected when the application
  // is started in an IDE (e.g. Visual Studio) or via dotnet run
  // Use environment variables in docker-compose.yaml to configure the
  // application running in the Docker environment!

  "ConnectionStrings": {
    "Postgres": "Host=localhost;Database=postgres;Username=postgres;Password=postgres"
  },
  "AppConfig": {
    "WebUri": "https://local.factorio.tech",
    //"ApiUri": "https://api.local.factorio.tech",
    "IdentityUri": "https://localhost:5001"
  },
  "DetailedErrors": true
}
